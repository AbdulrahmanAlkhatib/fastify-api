{
  "name": "first-demo",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "fastify start -l info --options server.js",
    "dev": "fastify start -l info --options server.js --watch --pretty-logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/auth": "^4.2.0",
    "@fastify/env": "^4.2.0",
    "@fastify/mongodb": "^6.2.0",
    "fastify": "^4.11.0",
    "fastify-cli": "^5.7.0",
    "fastify-plugin": "^4.4.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.8.2"
  }
}
